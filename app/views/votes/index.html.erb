<% if user_signed_in? %>
  <% if current_user.okregowy? %>
  <h1>Głosy w Twoim okręgu: </h1>



<table>
  <thead>
    <tr>
      <th>Ilość</th>
      <th>Komitet</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @votes.each do |vote| %>
      <tr>
        <td><%= vote.number %></td>
        <td><%= vote.committee.name%></td>
 
        <td><%= link_to 'Szczegóły', vote %></td>
        <td><%= link_to 'Edytuj', edit_vote_path(vote) %></td>
        <td><%= link_to 'Usuń', vote, method: :delete, data: { confirm: 'Przemyśl to jeszcze' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
  
<br>

<%= link_to 'Dodaj głosy', new_vote_path %>
   <% end %>    <% end %> 